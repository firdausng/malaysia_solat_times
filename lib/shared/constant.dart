import 'package:flutter/material.dart';

const kSolatTimeRemainStyle = TextStyle(
  fontSize: 20,
  color: Colors.white,
  shadows: [
    Shadow(
      color: Colors.green,
      blurRadius: 5,
      offset: Offset(2, 2),
    ),
  ],
);
